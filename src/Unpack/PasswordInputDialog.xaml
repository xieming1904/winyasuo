<ContentDialog
    x:Class="Unpack.PasswordInputDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Unpack"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="Enter Password"
    PrimaryButtonText="OK"
    CloseButtonText="Cancel"
    DefaultButton="Primary"
    PrimaryButtonClick="ContentDialog_PrimaryButtonClick"
    CloseButtonClick="ContentDialog_CloseButtonClick">

    <StackPanel Spacing="10">
        <PasswordBox x:Name="PasswordInputBox" Header="Password:" MinWidth="300"/>
        <TextBlock x:Name="InstructionTextBlock" Text="Enter the password for the archive." Foreground="{ThemeResource TextFillColorSecondaryBrush}" FontSize="12"/>
    </StackPanel>
</ContentDialog>
